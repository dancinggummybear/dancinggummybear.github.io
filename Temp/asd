<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>minMaxAvg() Assignment</title>
</head>
<body>
    <h1>Find minMaxAvg</h1>
    <h3>Find the minimum, maximum, and average of <u><b>array [1, 2]</b></u></h3>
    <div id='result'></div><br>

    <a href="http://https://github.com/dancinggummybear/dancinggummybear.github.io/commit/d4e298957c507cbe7ca483810e46e124824fce6b" target="_blank" rel="noopener noreferrer">Here is my Javascript code</a>

    <script src="js/minMaxAvg.js"></script>
    
</body>
</html>

const arr = getMinMaxAvg(1, 2)

function getMinMaxAvg(a, b){
 
  let min = Math.min(a, b)
  let Max = Math.max(a, b)
  let Avg = (a + b) / getMinMaxAvg.length
  
var minMaxAvg = ['Minimum = ' + min, ' Maximum = ' + Max, ' Average = ' + Avg]
console.log(minMaxAvg)

let elresult = document.getElementById('result')
elresult.innerHTML = 'The result is ' + minMaxAvg

} 